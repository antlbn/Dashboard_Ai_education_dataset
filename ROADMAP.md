# Roadmap: дашборд на новом стеке (для новичка)

Цель: быстро собрать интерактивный дашборд по AI Student Impact Dataset и параллельно выучить стек.
Принцип: **один новый инструмент за майлстоун**, учимся через дело, не пытаемся выучить всё сразу.

## Карта стека (зачем каждый инструмент)

| Инструмент | Роль в проекте |
|---|---|
| **Vite** | Сборщик и dev-сервер. Создаёт проект, мгновенный hot-reload. |
| **TypeScript** | Типы. Ловит ошибки до запуска, даёт автодополнение. |
| **Vue 3** | UI-фреймворк. Компоненты, реактивность. Сердце приложения. |
| **Pinia** | Хранилище состояния. Где лежат данные, фильтры, агрегаты. |
| **PrimeVue** | Готовые UI-компоненты (таблицы, дропдауны, карточки). |
| **shadcn-vue** | Набор красивых примитивов (+ Tailwind). Для финальной полировки. |
| **axios** | Загрузка данных по сети (JSON из `public/`). |
| **zod** | Проверка, что данные пришли в правильной форме + типы из схемы. |
| **Chart.js** | Графики (гистограммы, scatter, bar, doughnut). |

---

## M0 — Скелет проекта: Vite + Vue 3 + TS
**Учим:** что такое Vite/npm/node; структуру Vue-проекта; `<script setup>`; реактивность (`ref`, `computed`); компоненты; props/emits.
**Делаем:**
- `npm create vite@latest` → шаблон `vue-ts`, `npm install`, `npm run dev`.
- Разбить экран на layout: `Header`, `Sidebar` (фильтры), `Content` (карточки + графики) — пока пустые компоненты.
- Передать строку-проп из родителя в дочерний компонент, отрендерить.

**Готово, когда:** проект запускается, и ты умеешь создать компонент и передать в него данные.
**Это самый большой кусок обучения — не спеши.**

## M1 — Данные в приложении: axios + zod
**Учим:** `async/await`; axios `GET`; zod-схему; `parse` vs `safeParse`; вывод типов `z.infer<typeof schema>`.
**Делаем:**
- В Python один раз: `df.sample(5000).to_json('public/data.json', orient='records')` (сэмпл, чтобы браузеру было легко).
- Описать Zod-схему одной строки датасета (колонки см. `CLAUDE.md`).
- Через axios загрузить `data.json`, провалидировать массивом, вывести в консоль.

**Готово, когда:** в приложении есть провалидированные **типизированные** данные.

## M2 — Состояние: Pinia
**Учим:** store; `state` / `getters` / `actions`; как использовать store в компоненте.
**Делаем:**
- `dataStore`: хранит строки, статус загрузки (`idle/loading/error`); загрузку из M1 перенести в `action`.
- `filtersStore`: выбранные `Major_Category`, `Year_of_Study` и т.п.
- Getter `GPA_change` (Post − Pre) считать тут, не в данных.

**Готово, когда:** данные грузятся через store, компоненты читают из него.

## M3 — UI-каркас: PrimeVue (shadcn — позже)
**Учим:** установку/настройку PrimeVue, темы, импорт компонентов.
**Делаем:**
- KPI-карточки (`Card`): средний GPA, средние `Weekly_GenAI_Hours`, доля Burnout=High и т.п.
- Фильтры (`Select`/`Dropdown`) по `Major_Category` и `Year_of_Study`, привязанные к `filtersStore`.
- (Опционально) `DataTable` с сырыми строками.

**Готово, когда:** есть визуальный каркас, фильтры меняют состояние в store.

## M4 — Графики: Chart.js
**Учим:** `chart.js` + `vue-chartjs`; регистрацию компонентов; реактивное обновление при смене данных.
**Делаем графики прямо из находок EDA:**
- Гистограмма `Weekly_GenAI_Hours`.
- Bar: средний `GPA_change` по `Major_Category`.
- Scatter: `Weekly_GenAI_Hours` vs `GPA_change` — **подаём честно: связь почти нулевая (r ≈ −0.05), это не «драйвер» GPA** (см. CLAUDE.md).
- Doughnut: разбивка `Burnout_Risk_Level`.

**Готово, когда:** графики перерисовываются при смене фильтров.

## M5 — Интерактивность и связывание
**Учим:** агрегации через `computed`/getters; чтобы не пересчитывать на каждый рендер.
**Делаем:** фильтры → getters в Pinia пересчитывают агрегаты → обновляются и KPI, и графики. Единый живой дашборд.
**Готово, когда:** меняешь фильтр — реагирует весь экран.

## M6 — Полировка и деплой
**Делаем:** адаптивная вёрстка; состояния loading/empty/error (ошибку zod показать пользователю по-человечески); добавить **shadcn-vue** для финального вида; масштабировать данные с сэмпла до полного датасета (или предагрегировать в Python).
**Деплой:** `npm run build` → Netlify / Vercel / GitHub Pages.

---

## M7 — DataTable: интерактивность и самостоятельные фильтры

**Контекст:** таблица живёт отдельно от сайдбара — у неё своя логика фильтрации, не связанная с глобальными фильтрами дашборда. Сайдбар на этой странице убирается.

**Учим:** как PrimeVue DataTable управляет собственным состоянием фильтров (`v-model:filters`); разницу между глобальным поиском и колоночными фильтрами; шаблонные слоты (`#filter`) в DataTable.

**Делаем по шагам — переходим к следующему когда доволен результатом:**

1. **Убрать сайдбар со страницы таблицы** — скрыть или спрятать через `v-if` по активному виду. Принципиальный вопрос: как сообщить `AppSidebar` что сейчас открыта таблица — через `uiStore.activeView`.
2. **Сортировка по всем столбцам** — добавить `sortable` на все колонки, оценить визуал. Нравится / не нравится → решение о дальнейшей полировке.
3. **Глобальный текстовый поиск** — один `InputText` над таблицей, ищет по нескольким полям сразу. Оцениваем: плавность, понятность, визуал PrimeVue-инпута.
4. **Мультиселект по категориальным столбцам** — дропдаун с чекбоксами по `Major_Category`, `Year_of_Study`, `Burnout_Risk_Level` и т.д. Реализуется через `filterDisplay="menu"` + `MultiSelect` в слоте фильтра.

**Точка оценки после M7:** сравнить PrimeVue-таблицу с тем как выглядит shadcn DataTable (@tanstack/vue-table). Если PrimeVue устраивает — остаёмся. Если нет — M8 будет про замену.

**Готово, когда:** таблица работает автономно, сайдбар не мешает, поиск и фильтры понятны пользователю.

---

## M8 — Навигация: vue-router

**Контекст:** сейчас переключение между Overview / Distributions / Table — это `v-if` по `uiStore.activeView`. Нет URL, нет кнопки «Назад», нельзя скинуть ссылку на конкретную вкладку.

**Учим:** что такое клиентский роутинг (браузер не перезагружает страницу, JS меняет URL); разницу между `createWebHistory` (нужен сервер) и `createWebHashHistory` (работает на gh-pages); как `<router-view>` заменяет `v-if`; `router-link` vs обычная ссылка; что происходит с компонентом при навигации (mount/unmount) и почему это важно для Chart.js.

**Делаем:**
1. `npm install vue-router` → `src/router/index.ts` с тремя маршрутами (`/`, `/distributions`, `/table`).
2. `createWebHashHistory` — обязательно для gh-pages.
3. `<router-view>` в `AppContent.vue` вместо цепочки `v-if`.
4. `<router-link>` в `AppSidebar.vue` / навигационном меню вместо `ui.setView()`.
5. Удалить `uiStore` — он больше не нужен.
6. Lazy loading: каждый вид грузится только когда открыт (`() => import(...)`).

**Готово, когда:** URL меняется при навигации, кнопка «Назад» в браузере работает, ссылку на таблицу можно скинуть и она откроется.

---

## M9 — shadcn-vue: точечная полировка (опционально)

**Контекст:** принимается после M7. Если PrimeVue-таблица устраивает визуально — этот майлстоун пропускается или откладывается. Если нет — заменяем DataTable на @tanstack/vue-table + shadcn.

**Учим:** концепцию headless-компонентов (логика без UI); Tailwind CSS (классы вместо CSS-файлов); как shadcn копирует компоненты в проект и почему это не обычная npm-зависимость.

**Делаем (если решили идти):**
1. `npx shadcn-vue@latest init` → настройка Tailwind рядом с существующими CSS-переменными.
2. `npx shadcn-vue@latest add table input button badge` — добавляем только нужное.
3. DataTable переписывается на `@tanstack/vue-table` + shadcn `<Table>`.
4. Синхронизируем цветовые токены shadcn с существующей палитрой (`--bg-card`, `--border` и т.д.).

**Готово, когда:** таблица выглядит так как хочется, остальной дашборд не сломан.

---

## M10 — Финал: чистка и продакшн

**Делаем:**
- Удалить мёртвый код (неиспользуемые компоненты, закомментированные блоки).
- Проверить случайную выборку в скаттере (уже 1000 точек — ок).
- LOWESS: оценить скорость при фильтрации. Если тормозит — перенести в Web Worker.
- Обновить `README.md` — описание проекта, скриншоты, ссылка на gh-pages.
- Финальный `npm run deploy`.

---

## Порядок и темп
- Иди строго M0 → M10. Каждый майлстоун — рабочее состояние приложения.
- Переходи к следующему когда: разобрался в технологии хотя бы на уровне «понимаю что происходит» + доволен результатом в UI.
- Не форсируй M9 — shadcn нужен только если PrimeVue не устраивает визуально.
- Застрял — спрашивай по конкретному шагу, не пытайся объять всё.
